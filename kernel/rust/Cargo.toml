[package]
name = "safiros-kernel"
version = "0.1.0"
edition = "2021"

[features]
host-test = []

[lib]
crate-type = ["rlib", "staticlib"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
